# STM32F401RE Example

Example targeting the common ST Nucleo STM32F401RE dev board.

Running `cargo test` will cause Cargo to compile the test binary from `tests/`, and then relay it to `calico-cli` to flash it to the probe and run the tests.
